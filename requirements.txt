# Handoff runtime lock for Python 3.12.13. Update intentionally through a red
# dependency review; do not let an isolated LAN install silently float majors.
PyYAML==6.0.3
pydantic==2.13.4
click==8.4.2
