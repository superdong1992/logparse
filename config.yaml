schema_version: 2
pipeline:
  debug_expand_gz: false
  extraction_workers: auto
  diagnostic_scan_workers: auto
  keep_workspace: false
products:
  default:
    $include: configs/products/default.yaml
  compact:
    $include: configs/products/compact.yaml
