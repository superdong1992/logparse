[tool.ruff]
target-version = "py312"
line-length = 100

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.coverage.run]
branch = true
source = ["backend", "cli"]

[tool.coverage.report]
exclude_also = [
  "if TYPE_CHECKING:",
  '\.\.\.',
]
